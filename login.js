document.addEventListener('DOMContentLoaded',()=>{const f=document.querySelector('#loginForm'),m=document.querySelector('#msg');f.onsubmit=async e=>{e.preventDefault();m.textContent='Signing in…';const d=Object.fromEntries(new FormData(f));let email=d.identity;if(!email.includes('@')){const r=await fetch(`${SUPABASE_URL}/functions/v1/login-with-username`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:d.identity,password:d.password})});if(!r.ok){m.textContent='Invalid username or password.';return}const x=await r.json();email=x.email}const {error}=await db.auth.signInWithPassword({email,password:d.password});if(error)m.textContent=error.message;else location.href=new URLSearchParams(location.search).get('return')||'index.html'};document.querySelector('#google').onclick=async()=>{await db.auth.signInWithOAuth({provider:'google',options:{redirectTo:SITE+'index.html'}})}})
